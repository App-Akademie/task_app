import 'package:flutter/material.dart';

class S4424 extends StatelessWidget {
  const S4424({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
